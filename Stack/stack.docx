FileName:	stack.docx
author:	jonewan
date：		2018/11/22
堆栈
线性结构
堆栈的特点：
单一端输入、输出数据（FIFO）
堆栈具有记忆功能

数据在堆栈中的读和写都是在栈顶一段进行的。
有如下一组字符，先后入栈（push）和出栈（pop）
A、B、C、D、E、F
问，下面的输出顺序中，哪些是可能发生的，哪些是不可能发生的？
F、E、D、C、B、A（对）		A、B、C、D、E、F（对）
D、A、B、E、F、C（错）		D、C、E、B、F、A（对）
设有一个堆栈，一组数据顺序入栈和出栈，并按出栈顺序，形成如下内容：
B、E、F、D、C、A
问，这个栈的空间最小为多少？


堆栈的物理结构（实现方法）：
有效的控制一个堆栈，至少需要哪些量？
1、堆栈空间（真正用来存储用户数据的空间），我们用物理线性方式实现（即，采用数组），且是通过申请得到的
2、栈顶指针（这里的指针不同于c语言的指针概念，更像下标的概念）
3、堆栈存储空间最大值。
typedef struct STACK{
	USER_TYPE	*data;
	int stackMaxRoom;
	int top;
}STACK;

top永远指向栈顶元素的下一个空间；
